/var/log/iserverd/system.log {
    notifempty
    missingok
    create 644 iserverd iserverd
    compress
    postrotate
<<<<<<< ours
      [ -r /run/iserverd/iserverd.pid ] && kill -HUP "$(cat /run/iserverd/iserverd.pid)" 2>/dev/null || true
=======
        PIDFILE=/run/iserverd/iserverd.pid
        [ -r "$PIDFILE" ] || PIDFILE=/var/run/iserverd/iserverd.pid
        [ -r "$PIDFILE" ] && /bin/kill -HUP "$(cat "$PIDFILE")" || true
>>>>>>> theirs
    endscript
}

/var/log/iserverd/users.log {
    notifempty
    missingok
    create 644 iserverd iserverd
    compress
    postrotate
<<<<<<< ours
      [ -r /run/iserverd/iserverd.pid ] && kill -HUP "$(cat /run/iserverd/iserverd.pid)" 2>/dev/null || true
=======
        PIDFILE=/run/iserverd/iserverd.pid
        [ -r "$PIDFILE" ] || PIDFILE=/var/run/iserverd/iserverd.pid
        [ -r "$PIDFILE" ] && /bin/kill -HUP "$(cat "$PIDFILE")" || true
>>>>>>> theirs
    endscript
}

/var/log/iserverd/alarm.log {
    notifempty
    missingok
    create 644 iserverd iserverd
    compress
    postrotate
<<<<<<< ours
      [ -r /run/iserverd/iserverd.pid ] && kill -HUP "$(cat /run/iserverd/iserverd.pid)" 2>/dev/null || true
=======
        PIDFILE=/run/iserverd/iserverd.pid
        [ -r "$PIDFILE" ] || PIDFILE=/var/run/iserverd/iserverd.pid
        [ -r "$PIDFILE" ] && /bin/kill -HUP "$(cat "$PIDFILE")" || true
>>>>>>> theirs
    endscript
}

/var/log/iserverd/debug.log {
    notifempty
    missingok
    create 644 iserverd iserverd
    compress
    postrotate
<<<<<<< ours
      [ -r /run/iserverd/iserverd.pid ] && kill -HUP "$(cat /run/iserverd/iserverd.pid)" 2>/dev/null || true
=======
        PIDFILE=/run/iserverd/iserverd.pid
        [ -r "$PIDFILE" ] || PIDFILE=/var/run/iserverd/iserverd.pid
        [ -r "$PIDFILE" ] && /bin/kill -HUP "$(cat "$PIDFILE")" || true
>>>>>>> theirs
    endscript
}
